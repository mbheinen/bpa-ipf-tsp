c
      integer MAXGENDRP2
      parameter (MAXGENDRP2 = 1200)
      common /comdrx/ oldrop, pctpku, pold(MAXGENDRP2),
     &                itrtot, slkgen(MAXCAR)
c
      double precision oldrop, pctpku, pold
      real slkgen
      integer itrtot

